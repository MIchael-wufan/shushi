"""教学图视觉开关（**仅此文件**手改，勿从环境变量/CLI 注入）。

- `REMARKS_IN_RED`：除法移位/补零/新小数点、验算块内强调、乘除尾零/小数点等「标注色」
- `SHOW_CARRY_BORROW_DIGITS`：是否**绘制**进位/借位小号过程数字（不绘制时与「无进位」外观一致）
- `CARRY_BORROW_IN_RED`：在已绘制时，进位/借位是否用强调色
"""

# =============================================================================
# 开关（默认：不画进位/借位小号字；不额外用红标教学色）
# =============================================================================

# 为 True 时，教学标注使用 Style.color_red（#e02020）；
# 为 False 时，上述标注与主文同色（不额外高亮为红）。
REMARKS_IN_RED: bool = False

# 为 True 时，才绘制加法/减法/乘法竖式中的进位、借位小号字；默认不绘制。
SHOW_CARRY_BORROW_DIGITS: bool = False

# 仅在 `SHOW_CARRY_BORROW_DIGITS` 为 True 时生效：为 True 时进位/借位用 color_red，否则与主文同色。
CARRY_BORROW_IN_RED: bool = False
